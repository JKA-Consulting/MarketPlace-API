# MarketPlace-API

This repository holds the codes to activate all required APIs to deploy an application on Google Kubernetes Engine (GKE).
It comprises of a terraform script to activate the APIs and a cloudbuild file to automatically deploy the terraform script on Google Cloud
